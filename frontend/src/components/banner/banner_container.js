import { connect } from "react-redux";
import Banner from "./banner";

const mSTP = start => {
  return {

  };
};

const mDTP = dispatch => {
  return {

  };
};

export default connect(mSTP, mDTP)(Banner);